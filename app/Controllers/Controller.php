<?php

namespace App\Controllers;

/**
 * Base Controller
 * Chứa các logic dùng chung cho toàn bộ các Controller khác trong hệ thống.
 */
abstract class Controller {
    // Bạn có thể thêm các method bổ trợ như middleware(), validate(), jsonResponse() ở đây.
}

